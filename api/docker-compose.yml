services:
  api:
    build: .
    ports:
      - "3002:3002"
    volumes:
      - shelflet-data:/data
    environment:
      - PORT=3002
      - DB_PATH=/data/shelflet.db
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  shelflet-data:
